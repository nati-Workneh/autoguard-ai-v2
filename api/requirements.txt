# Scoped dependencies for the Vercel serverless function only.
# Intentionally excludes torch/gradio/jupyter/matplotlib/seaborn: the deployed
# model (model_v2.pkl) is a scikit-learn Pipeline (Random Forest), not a
# PyTorch network, so no PyTorch runtime is needed to unpickle or serve it.
# If the notebook is re-run and a PyTorch network wins Validation selection,
# add torch==2.12.1 here before redeploying.
fastapi==0.136.1
pydantic==2.13.3
joblib==1.5.3
pandas==3.0.3
numpy==2.4.4
scikit-learn==1.9.0
