# Sprint 3.4 changelog

The ML artifact was frozen and its SHA-256 was preserved. The frontend now displays the API-provided `business_action`; it contains no routing threshold logic. The backend’s shared business policy remains the operational source, separate from presentation risk bands. Strict JSON outputs use `null` for unavailable payback.
